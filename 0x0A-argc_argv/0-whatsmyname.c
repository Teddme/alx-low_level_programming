#include <stdio.h>

/**
 * main - prt ex. prg name
 * @argc: # of arg
 * @argv: array of arg
 * Return: rslt.
 */
int main(int __attribute__((__unused__))  argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
