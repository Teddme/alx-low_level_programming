#include <stdio.h>

/**
 * main - prt ex. prg name
 * @argc: # of arg
 * @argv: array of arg
 * Return: rslt.
 */
int main(int argc, char __attribute__((__unused__)) *argv[])
{
	printf("%d\n", argc - 1);

	return (0);
}
